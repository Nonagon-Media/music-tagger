# Music Tagger Configuration

# Required: Path to your music library
MUSIC_DIR=/path/to/your/music

# Required: AcoustID API key (free at https://acoustid.org/new-application)
ACOUSTID_API_KEY=your_api_key_here

# Optional: Confidence threshold for auto-approval (0-100, default: 80)
CONFIDENCE_THRESHOLD=80

# Optional: Delay between API calls in seconds (default: 0.5)
RATE_LIMIT_DELAY=0.5

# Optional: Delay between tag writes in seconds (default: 0.5)
WRITE_DELAY=0.5

# Optional: Port for rq-dashboard (default: 9181)
DASHBOARD_PORT=9181

# Optional: Port for review UI (default: 8080)
UI_PORT=8080
